
function Rectangle(x, y, w, h) {
  this.x = x;
  this.y = y;
  this.w = w;
  this.h = h;
};

Rectangle.prototype = {
  getOverlap: function getOverlap(other) {
    // TODO if (this.x )
  },
};

module.exports = Rectangle;